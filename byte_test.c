#include <stdio.h>
int main()
{
    printf("int size: %zu bytes\n", sizeof(int));
    printf("char size: %zu byte\n", sizeof(char));
    printf("float size: %zu bytes\n", sizeof(float));
    return 0;
}